--- ./discord_94011/src/main/java/com/discord/widgets/user/profile/UserProfileHeaderView.java	2021-12-24 01:10:15.536027100 +0400
+++ ./bluecord/src/main/java/com/discord/widgets/user/profile/UserProfileHeaderView.java	2021-12-24 01:08:35.948264000 +0400
@@ -57,6 +57,8 @@ import kotlin.jvm.functions.Function1;
 import kotlin.jvm.functions.Function2;
 import kotlin.jvm.internal.DefaultConstructorMarker;
 import kotlin.jvm.internal.Ref$BooleanRef;
+import mods.utils.DevBadge;
+import mods.view.SheetConfig;
 import rx.Observable;
 import rx.Subscription;
 
@@ -66,6 +68,7 @@ public final class UserProfileHeaderView
     public static final Companion Companion = new Companion(null);
     private final SimpleRecyclerAdapter<Badge, BadgeViewHolder> badgesAdapter;
     private MGImages.DistinctChangeDetector bannerChangeDetector;
+    public String bannerUrl;
     private final UserProfileHeaderViewBinding binding;
     private Function0<Unit> onAvatarEdit;
     private Function1<? super Integer, Unit> onAvatarRepresentativeColorUpdated;
@@ -240,7 +243,9 @@ public final class UserProfileHeaderView
         int max = Math.max(i, resources2.getDisplayMetrics().heightPixels);
         int i2 = 0;
         if (banner == null || !t.startsWith$default(banner, "data:", false, 2, null)) {
-            str = IconUtils.INSTANCE.getForUserBanner(loaded.getUser().getId(), banner, Integer.valueOf(max), loaded.getShouldAnimateBanner());
+            String forUserBanner = IconUtils.INSTANCE.getForUserBanner(loaded.getUser().getId(), banner, Integer.valueOf(max), loaded.getShouldAnimateBanner());
+            this.bannerUrl = forUserBanner;
+            str = forUserBanner;
         } else {
             str = banner;
         }
@@ -275,8 +280,14 @@ public final class UserProfileHeaderView
         User user = loaded.getUser();
         GuildMember guildMember = loaded.getGuildMember();
         int dimension = (int) getResources().getDimension(2131165293);
-        this.binding.j.b(getPrimaryNameTextForUser(user, guildMember), loaded.getShowMediumAvatar() ? IconUtils.getForUser(loaded.getUser(), false, Integer.valueOf(dimension)) : null, false, Integer.valueOf(dimension), Integer.valueOf(this.userProfileHeaderBackgroundColor));
-        this.binding.j.a(user.isBot(), user.isSystemUser() ? 2131894875 : 2131887108, UserUtils.INSTANCE.isVerifiedBot(user));
+        String forUser = loaded.getShowMediumAvatar() ? IconUtils.getForUser(loaded.getUser(), false, Integer.valueOf(dimension)) : null;
+        UsernameView usernameView = this.binding.j;
+        SpannableStringBuilder primaryNameTextForUser = getPrimaryNameTextForUser(user, guildMember);
+        SheetConfig.addUserDetails(user, guildMember, primaryNameTextForUser);
+        usernameView.b(primaryNameTextForUser, forUser, false, Integer.valueOf(dimension), Integer.valueOf(this.userProfileHeaderBackgroundColor));
+        UsernameView usernameView2 = this.binding.j;
+        usernameView2.a(user.isBot(), user.isSystemUser() ? 2131894875 : 2131887108, UserUtils.INSTANCE.isVerifiedBot(user));
+        DevBadge.add(usernameView2, user.getId());
     }
 
     private final void configureSecondaryName(UserProfileHeaderViewModel.ViewState.Loaded loaded) {
