--- ./discord_94011/src/main/java/com/discord/utilities/time/TimeUtils.java	2021-12-24 01:10:13.524023400 +0400
+++ ./bluecord/src/main/java/com/discord/utilities/time/TimeUtils.java	2021-12-24 01:08:33.917263000 +0400
@@ -15,6 +15,7 @@ import java.util.Date;
 import java.util.Locale;
 import java.util.TimeZone;
 import java.util.concurrent.TimeUnit;
+import mods.ThemingTools;
 
 public final class TimeUtils {
     private static final String DATE_FORMAT_DISCORD_API;
@@ -142,6 +143,10 @@ public final class TimeUtils {
     public static final CharSequence toReadableTimeString(Context context, long j, Clock clock) {
         m.checkNotNullParameter(context, "context");
         m.checkNotNullParameter(clock, "clock");
+        String exactStamps = ThemingTools.exactStamps(j);
+        if (exactStamps != null) {
+            return exactStamps;
+        }
         LocaleManager localeManager2 = localeManager;
         if (m.areEqual(localeManager2.getPrimaryLocale(context).getLanguage(), "en")) {
             return INSTANCE.toReadableTimeStringEN(localeManager2.getPrimaryLocale(context), j, clock);
